import React from 'react';
import Counter from './features/counter/Counter';

const App: React.FC = () => {
    return (
        <div>
            <h1>My Vite App</h1>
            <Counter />
        </div>
    );
};

export default App;
